---
import Layout from "../layouts/Layout.astro";

import Hero from "../components/Hero.astro";
import SectionTitle from "../components/SectionTitle.astro";

import { Image } from "astro:assets";
import websiteDevelopment from "../assets/services/website-development.jpg";
import aiIntegration from "../assets/services/ai-integration.jpg";
import seoOptimization from "../assets/services/seo-optimization.jpg";
import ecommerceSolutions from "../assets/services/ecommerce-solutions.jpg";

const services = [
    {
        title: "Website Development",
        description: "Custom websites tailored to your business needs.",
        image: websiteDevelopment,
    },
    {
        title: "AI Integration",
        description: "Integrate AI solutions into your websites.",
        image: aiIntegration,
    },
    {
        title: "Ecommerce Solutions",
        description:
            "Comprehensive ecommerce solutions to grow your online store.",
        image: ecommerceSolutions,
    },
    {
        title: "SEO Optimization",
        description: "Boost your online presence with our SEO services.",
        image: seoOptimization,
    },
];

const faq = [
    {
        question:
            "What makes VXIT different from other web development agencies?",
        answer: "VXIT specializes in creating high-performance, custom websites with cutting-edge AI integration. We focus on delivering sleek designs that drive real business growth, not just pretty websites.",
    },
    {
        question: "How long does a typical project take?",
        answer: "Project timelines vary based on complexity. A standard website takes 2-4 weeks, while custom AI integrations or complex ecommerce solutions may take 6-8 weeks. We'll provide a detailed timeline during consultation.",
    },
    {
        question: "Do you provide ongoing support and maintenance?",
        answer: "Absolutely! We offer comprehensive post-launch support including updates, security monitoring, performance optimization, and technical assistance to ensure your site continues to perform at its best.",
    },
    {
        question: "What's included in your AI integration services?",
        answer: "Our AI integration services include chatbots, automated content generation, personalized user experiences, predictive analytics, and custom AI tools tailored to your business needs.",
    },
    {
        question: "Can you help improve my existing website's performance?",
        answer: "Yes! We offer website audits, performance optimization, SEO improvements, and modernization services to transform your existing site into a high-performing digital asset.",
    },
    {
        question: "What's your pricing structure?",
        answer: "We offer flexible pricing based on project scope and requirements. Contact us for a free consultation where we'll provide a detailed quote tailored to your specific needs and budget.",
    },
];
---

<Layout title="Home - VXIT">
    <main>
        <Hero
            title="Creative Excellence"
            subtitle="VXIT engineers sleek, high-performing sites that drive growth. We handle the tech, so you can focus on your success."
            cta
        />
        <section class="about-us">
            <SectionTitle>About Us</SectionTitle>
            <p id="animatedParagraph">
                <span>VXIT</span>
                <span>is</span>
                <span>dedicated</span>
                <span>to</span>
                <span>delivering</span>
                <span>high-quality</span>
                <span>digital</span>
                <span>solutions</span>
                <span>that</span>
                <span>empower</span>
                <span>businesses</span>
                <span>to</span>
                <span>thrive</span>
                <span>in</span>
                <span>the</span>
                <span>digital</span>
                <span>landscape.</span>
                <span>Our</span>
                <span>team</span>
                <span>of</span>
                <span>experts</span>
                <span>is</span>
                <span>committed</span>
                <span>to</span>
                <span>excellence,</span>
                <span>innovation,</span>
                <span>and</span>
                <span>customer</span>
                <span>satisfaction.</span>
            </p>
        </section>
        <section>
            <SectionTitle>Achievements</SectionTitle>
            <div class="stats">
                <div class="stats-grid" data-aos="fade-up">
                    <div class="stat-card">
                        <p class="stat-number">150</p>
                        <p class="stat-label">Projects Completed</p>
                    </div>
                    <div class="stat-card" data-aos="fade-up">
                        <p class="stat-number">99%</p>
                        <p class="stat-label">Uptime Guarantee</p>
                    </div>
                    <div class="stat-card" data-aos="fade-up">
                        <p class="stat-number">24h</p>
                        <p class="stat-label">Support Response</p>
                    </div>
                    <div class="stat-card" data-aos="fade-up">
                        <p class="stat-number">98%</p>
                        <p class="stat-label">Client Satisfaction</p>
                    </div>
                </div>
                <div class="reviews">
                    <div class="review-card" data-aos="flip-up">
                        <div class="stars">
                            <i class="fas fa-star"></i>
                            <i class="fas fa-star"></i>
                            <i class="fas fa-star"></i>
                            <i class="fas fa-star"></i>
                            <i class="fas fa-star"></i>
                        </div>
                        <p>
                            "VXIT transformed our online presence completely.
                            The AI integration they implemented increased our
                            customer engagement by 300%."
                        </p>
                        <div class="review-author">
                            <p>
                                <strong>Sarah Chen</strong><br />
                                CEO, TechFlow Solutions
                            </p>
                        </div>
                    </div>
                    <div class="review-card" data-aos="flip-up">
                        <div class="stars">
                            <i class="fas fa-star"></i>
                            <i class="fas fa-star"></i>
                            <i class="fas fa-star"></i>
                            <i class="fas fa-star"></i>
                            <i class="fas fa-star"></i>
                        </div>
                        <p>
                            "Outstanding service and attention to detail. Our
                            e-commerce sales doubled within 3 months of
                            launching the new site."
                        </p>
                        <div class="review-author">
                            <p>
                                <strong>Marcus Rodriguez</strong><br />
                                Founder, EcoMarket
                            </p>
                        </div>
                    </div>
                    <div class="review-card" data-aos="flip-up">
                        <div class="stars">
                            <i class="fas fa-star"></i>
                            <i class="fas fa-star"></i>
                            <i class="fas fa-star"></i>
                            <i class="fas fa-star"></i>
                            <i class="fas fa-star"></i>
                        </div>
                        <p>
                            "The team's expertise in modern web technologies is
                            unmatched. They delivered beyond our expectations."
                        </p>
                        <div class="review-author">
                            <p>
                                <strong>Dr. Emily Watson</strong><br />
                                Director, MedTech Innovations
                            </p>
                        </div>
                    </div>
                </div>
            </div>
        </section>
        <section>
            <SectionTitle>Our Services</SectionTitle>
            <div class="services">
                {
                    services.map((service) => (
                        <a href="/vxit/services">
                            <div class="service-card">
                                <Image
                                    src={service.image}
                                    alt={service.title}
                                />
                                <div class="service-info">
                                    <i class="fa-solid fa-arrow-up-right-from-square" />
                                    <h3>{service.title}</h3>
                                    <p>{service.description}</p>
                                </div>
                            </div>
                        </a>
                    ))
                }
            </div>
        </section>
        <section>
            <SectionTitle>Frequently Asked Questions</SectionTitle>
            <div class="faq">
                {
                    faq.map((item, index) => (
                        <div class="faq-item">
                            <button class="faq-question" data-index={index}>
                                <span>{item.question}</span>
                                <i class="fa-solid fa-chevron-down" />
                            </button>
                            <div class="faq-answer">
                                <p>{item.answer}</p>
                            </div>
                        </div>
                    ))
                }
            </div>
        </section>
    </main>
</Layout>

<style>
    /* --- Services Section --- */
    .services {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(500px, 1fr));
        max-width: 1400px;
        margin: 0 auto;
    }
    .services a {
        display: block;
        text-decoration: none;
        color: inherit;
    }

    .service-card {
        overflow: hidden;
        position: relative;
        height: 400px;
        background: var(--color-black);
        border: 1px solid var(--color-gray);
        cursor: pointer;
    }
    .service-card img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        opacity: 0;
        transition: opacity 0.4s ease;
    }
    .service-card:hover img {
        opacity: 1;
    }
    .service-info {
        position: absolute;
        top: 0;
        left: 0;
        width: calc(100% - 2 * var(--space-6));
        height: calc(100% - 2 * var(--space-6));
        display: flex;
        flex-direction: column;
        justify-content: end;
        padding: var(--space-6);
        background: rgba(0, 0, 0, 0.5);
    }
    .service-info i {
        position: absolute;
        top: var(--space-6);
        right: var(--space-6);
        font-size: var(--font-size-xl);
        transform: translateX(20px);
        transition:
            transform 0.4s ease,
            opacity 0.4s ease;
        opacity: 0;
    }
    .service-info h3 {
        position: absolute;
        font-size: var(--font-size-3xl);
        opacity: 0.5;
        transition:
            transform 0.4s ease,
            opacity 0.4s ease;
    }
    .service-info p {
        font-size: var(--font-size-lg);
        opacity: 0;
        transform: translateY(40px);
        transition:
            transform 0.4s ease,
            opacity 0.4s ease;
    }
    .service-info:hover h3 {
        transform: translateY(-40px);
        opacity: 0.8;
    }
    .service-info:hover p {
        display: block;
        opacity: 0.8;
        transform: translateY(-5px);
    }
    .service-info:hover i {
        transform: translateX(0);
        opacity: 1;
    }

    /* --- About Us Section --- */
    .about-us {
        width: calc(100% - 2 * var(--space-6));
        padding: var(--space-6);
    }
    .about-us p {
        max-width: 1200px;
        margin: auto;
        font-size: var(--font-size-4xl);
        word-break: break-word;
    }
    .about-us p span {
        opacity: 0.25;
        transition: opacity 0.5s;
        display: inline-block;
    }

    /* --- Stats Section --- */
    .stats {
        display: flex;
        flex-direction: column;
        gap: var(--space-16);
        max-width: 1200px;
        margin: 0 auto;
        padding: var(--space-6);
    }

    .stats-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: var(--space-6);
    }

    .stat-card {
        display: flex;
        gap: var(--space-2);
        flex-direction: column;
        justify-content: center;
        align-items: center;
        padding: var(--space-8) var(--space-6);
        border: 1px solid var(--color-gray);
        border-radius: var(--radius-2xl);
        transition: all 0.4s ease;
    }
    .stat-card:hover {
        transform: translateY(-8px);
        border-color: rgba(var(--color-accent-rgb), 0.5);
        box-shadow: 0 20px 40px rgba(var(--color-accent-rgb), 0.2);
    }

    .stat-number {
        font-size: var(--font-size-6xl);
        font-weight: var(--font-weight-bold);
        background: linear-gradient(
            225deg,
            var(--color-accent) 0%,
            #4000ff 100%
        );
        background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    .stat-label {
        font-size: var(--font-size-xl);
    }

    /* --- Reviews Section --- */
    .reviews {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: var(--space-6);
    }

    .review-card {
        display: flex;
        flex-direction: column;
        gap: var(--space-6);
        position: relative;
        background: var(--color-black);
        border: 1px solid var(--color-gray);
        border-radius: var(--radius-2xl);
        padding: var(--space-8);
        transition: all 0.4s ease;
    }
    .review-card::before {
        content: "";
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: linear-gradient(
            135deg,
            rgba(255, 255, 255, 0.1) 0%,
            transparent 50%,
            rgba(var(--color-accent-rgb), 0.1) 100%
        );
        border-radius: var(--radius-2xl);
        opacity: 0;
        transition: opacity 0.4s ease;
    }
    .review-card:hover {
        transform: translateY(-4px);
        border-color: rgba(255, 255, 255, 0.2);
        box-shadow: 0 15px 30px rgba(var(--color-accent-rgb), 0.2);
    }
    .review-card:hover::before {
        opacity: 1;
    }

    .stars {
        display: flex;
        gap: var(--space-1);
    }
    .stars i {
        color: #ffd700;
        filter: drop-shadow(0 0 4px rgba(255, 215, 0, 0.3));
    }

    .review-card p {
        font-size: var(--font-size-lg);
        line-height: var(--line-height-relaxed);
        color: var(--color-white);
        font-style: italic;
    }
    .review-author strong {
        font-size: var(--font-size-xl);
    }

    /* --- FAQ Section --- */
    .faq {
        display: flex;
        flex-direction: column;
        gap: var(--space-2);
        max-width: 900px;
        margin: 0 auto;
        padding: 0 var(--space-6);
    }
    .faq-item {
        border-bottom: 1px solid var(--color-gray);
    }

    .faq-question {
        width: 100%;
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: var(--space-6) 0;
        background: none;
        border: none;
        color: inherit;
        cursor: pointer;
        font-size: var(--font-size-2xl);
        font-weight: var(--font-weight-semibold);
        text-align: left;
        transition: color 0.3s ease;
    }
    .faq-question:hover {
        color: var(--color-accent);
    }
    .faq-question i {
        font-size: var(--font-size-lg);
        transition: transform 0.3s ease;
        color: var(--color-accent);
    }

    .faq-answer {
        max-height: 0;
        overflow: hidden;
        transition:
            max-height 0.3s ease,
            padding 0.3s ease;
    }
    .faq-answer p {
        font-size: var(--font-size-lg);
        line-height: 1.6;
        color: rgba(255, 255, 255, 0.8);
        margin: 0;
    }

    .faq-item.active .faq-question i {
        transform: rotate(180deg);
    }
    .faq-item.active .faq-answer {
        max-height: 200px;
        padding-bottom: var(--space-6);
    }

    @media (max-width: 768px) {
        .about-us p {
            font-size: var(--font-size-3xl);
        }

        .services {
            grid-template-columns: 1fr;
        }
        .service-card img {
            opacity: 1;
        }
        .service-info {
            gap: var(--space-2);
        }
        .service-info h3,
        .service-info p {
            position: static;
            opacity: 0.8;
            transform: translate(0);
        }
        .service-info i {
            opacity: 1;
            transform: translate(0);
        }
        .service-info:hover * {
            transform: translate(0);
            opacity: 1;
        }

        .stats-grid {
            gap: var(--space-4);
        }
        .stat-card {
            padding: var(--space-6) var(--space-4);
        }
        .stat-number {
            font-size: var(--font-size-5xl);
        }
        .stat-suffix {
            font-size: var(--font-size-2xl);
        }
    }
</style>
<script>
    // Word-by-word scroll reveal for About Us
    window.addEventListener("scroll", () => {
        const p = document.querySelector("#animatedParagraph");
        if (!p) return;
        const words = p.querySelectorAll("#animatedParagraph span");

        const rect = p.getBoundingClientRect();
        const windowHeight =
            window.innerHeight || document.documentElement.clientHeight;

        if (rect.top < windowHeight && rect.bottom > 0) {
            // Calculate how much of the paragraph is visible
            const visibleRatio = Math.min(
                1,
                (windowHeight - rect.top - 250) / rect.height,
            );
            const revealCount = Math.floor(words.length * visibleRatio);
            words.forEach((word, i) => {
                (word as HTMLElement).style.opacity =
                    i < revealCount ? "1" : "0.25";
            });
        } else {
            words.forEach(
                (word) => ((word as HTMLElement).style.opacity = "0.25"),
            );
        }
    });

    // FAQ accordion functionality
    document.addEventListener("DOMContentLoaded", () => {
        const faqQuestions = document.querySelectorAll(".faq-question");

        faqQuestions.forEach((question) => {
            question.addEventListener("click", () => {
                const faqItem = question.parentElement;
                if (!faqItem) return;

                const isActive = faqItem.classList.contains("active");

                // Close all other FAQ items
                document.querySelectorAll(".faq-item").forEach((item) => {
                    item.classList.remove("active");
                });

                // Toggle current item
                if (!isActive) {
                    faqItem.classList.add("active");
                }
            });
        });
    });
</script>
